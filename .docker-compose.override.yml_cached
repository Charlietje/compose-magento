version: '3'

services:

  mailcatcher:
    ports:
      - 1080:1080


  nginx:
    volumes:
      - ${APPLICATION:-../magento}:/phpapp:cached

  application:
    volumes:
      - ${APPLICATION:-../magento}:/phpapp:cached


  elastic-hq:
    ports:
      - 5000:5000


  nginx_ssl:
    ports:
      - 80:80
      - 443:443

